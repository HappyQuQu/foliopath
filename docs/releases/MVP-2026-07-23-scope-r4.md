# MVP-2026-07-23 Scope Manifest — Revision 4

## 冻结记录

- 版本：`MVP-2026-07-23`
- Scope revision：`4`
- 状态：`Scope Frozen`
- 冻结日期：2026-07-30
- 基线：revision 3 + [CR-2026-009](../changes/CR-2026-009-frontend-prototype-fidelity.md)
- 替代：[revision 3](MVP-2026-07-23-scope-r3.md)
- Feature：[FTR-UIF-001](../features/frontend-prototype-fidelity.md)
- Scope-budget exception：产品负责人接受生产原型一致性 feature 的增量 API、后端、前端和
  测试预算，并指定其为当前首要任务

## 范围

Revision 3 的全部 FR、NFR、非目标和验收继续有效，并新增：

- `FR-BRW-010`：当前目录关键字对可靠索引中的全部直接子目录和当前媒体范围生效，使用服务端
  查询、稳定 cursor 和 URL 恢复，不只过滤已加载项。
- `FR-UI-010`：生产认证后页面使用已确认原型的全局 Header、共享壳、管理导航、视觉 token
  和响应式层级，不出现重复 Header/主导航或无行为静态控件。
- `NFR-UIF-001`：四档同状态视觉比较关闭全部 P0/P1/P2，主要区域几何偏差不超过 2px，并
  建立阻断式视觉回归。

Revision 3 的 `FR-AUTH-005` 和 `FR-UI-009` 纳入 `FTR-UIF-001` 统一交付，不改变其语义。

## 明确边界

- 包含最小缓存摘要与安全清理，用于完成已批准的扫描与缓存独立页面；
- 不包含缓存 missing/all rebuild、统一任务中心、系统维护、完整性、应用备份和诊断包；
- 不包含 AI 搜索、OCR、人脸识别、多用户、上传、转码或新部署单元；
- `MVP-NG-001～008` 继续有效；
- 原始媒体只读、安全、容量和发布 Gate 不能作为 scope-budget 交换项。

## 增量验收

- `UIF-AC-001～012` 全部通过；
- account、directory q、cache cleanup 达到 Backend Evidence Ready 后才接生产业务 UI；
- 1440×900、1265×800、768×1024、390×844 的中英文、浅色/深色主矩阵无 P0/P1/P2；
- 浏览页顶部/中段/底部无额外空白、嵌套滚动或隐藏持续控制；
- 100k/10k 档不出现全量客户端目录加载或无界 DOM；
- 原媒体 sentinel、密码/路径/错误脱敏和三浏览器回归通过；
- `UIF-S4 Integrated Slice Done` 后重跑受影响 Stage 5 Gate。

版本完成条件沿用 revision 1；本 revision 不把当前候选标记为 Released。
