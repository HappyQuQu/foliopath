# MVP-2026-07-23 Scope Manifest — Revision 2

## 冻结记录

- 版本：`MVP-2026-07-23`
- Scope revision：`2`
- 状态：`Scope Frozen`
- 冻结日期：2026-07-28
- 基线：revision 1 + [CR-2026-002](../changes/CR-2026-002-authenticated-lan-http.md)
- 替代：[revision 1](MVP-2026-07-23-scope.md)
- Scope-budget exception：经认证 LAN HTTP 替换强制代理部署，不增加应用部署单元

## 范围

Revision 1 的全部 FR、NFR、非目标和验收继续有效，并新增：

- `FR-DEP-005`：推荐 Compose 支持经单管理员认证的受信局域网 HTTP 直连；公网 TLS 与
  反向代理由部署者负责。

`MVP-NG-001～008` 保持不变；匿名 LAN、多用户、分享链接仍不属于 MVP。

## 增量验收

- 推荐 Compose 在未配置可信代理时可从另一台 LAN 客户端完成 auth status、首次设置、登录和
  受保护 API 访问。
- 未受信直连请求提交的转发头不能改变 scheme、authority 或限流客户端身份。
- 显式可信代理配置继续执行严格单跳 HTTPS 校验并拒绝旁路。
- `FOLIOPATH_BIND_ADDRESS=127.0.0.1` 可恢复仅回环发布。

版本完成条件沿用 revision 1；本 revision 不把当前候选标记为 Released。
