# MVP-2026-07-23 Scope Manifest — Revision 3

## 冻结记录

- 版本：`MVP-2026-07-23`
- Scope revision：`3`
- 状态：`Scope Frozen`
- 冻结日期：2026-07-30
- 基线：revision 2 + [CR-2026-006](../changes/CR-2026-006-management-center-pages.md)
- 替代：[revision 2](MVP-2026-07-23-scope-r2.md)
- Scope-budget exception：产品负责人接受管理中心独立页面和单管理员账户维护的增量 UI/API/测试预算

## 范围

Revision 2 的全部 FR、NFR、非目标和验收继续有效，并新增：

- `FR-AUTH-005`：唯一管理员可修改显示名称与密码；密码修改验证当前密码和确认值，并使其他
  会话重新认证。
- `FR-UI-009`：通用、媒体库、扫描与缓存、账户使用四个独立、可直达且具备完整交互状态的页面。

`FR-UI-008` 继续属于 `POST-MVP-1` 视频故事板悬停预览，不被本 revision 复用或改变。
`MVP-NG-001～008` 保持不变；多管理员、角色权限、邮件恢复和外部身份提供商仍不属于 MVP。

## 增量验收

- 四个管理分类各有独立路由，刷新、前进后退和复制地址恢复当前分类。
- 设置表单显示脏状态，保存成功清除；无效或并发冲突保留输入并给出可恢复错误。
- 媒体库、扫描和缓存页面覆盖既有生命周期与故障状态，不新增原媒体写入能力。
- 修改密码验证当前密码、8 个 Unicode 字符下限和一致确认；成功后撤销其他会话。
- 桌面与移动宽度下核心操作均可由键盘完成，状态不只依赖颜色。

版本完成条件沿用 revision 1；本 revision 不把当前候选标记为 Released。账户生产实现仍受
OpenAPI、后端集成、安全和 Stage 5 回归 Gate 阻断。
