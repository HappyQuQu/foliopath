# FIX-2026-07-31：Header 快速语言切换

- 类型：已批准应用壳/UIF 切片内的例行交互优化
- 需求与版本：`FR-UI-001～007`，MVP Stage 5；不扩大已冻结的中英双语范围
- Gate：沿用已交付的双语与认证后全局 Header，不新增后端或 API 合同
- Owner：前端共享 UI；`LocaleProvider` 仍是语言状态、持久化和切换行为的唯一 owner
- 影响：通用设置和所有认证后页面的全局 Header
- 行为：通用设置继续提供“跟随浏览器 / 简体中文 / English”；账户菜单左侧增加共享语言图标按钮，可直接在简体中文与英文间切换
- 一致性：快捷切换写入显式语言偏好；在通用设置页使用快捷按钮时，语言字段同步为当前偏好
- 可访问性：按钮使用随当前语言变化的操作标签，支持键盘焦点，装饰图标不进入可访问树
- 回归：共享组件测试验证持久化和 `html[lang]`；应用壳测试验证按钮顺序；设置页测试验证双入口同步
