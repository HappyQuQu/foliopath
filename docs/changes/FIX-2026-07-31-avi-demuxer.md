# FIX-2026-07-31：AVI 解封装与失败任务重试

- 类型：`CR-2026-010` AVI 支持切片内的发布镜像缺陷修复
- 根因：扫描器已识别 AVI，但精简 FFmpeg 构建未启用 `avi` demuxer，导致所有 AVI 探测为
  `invalid_media`
- 修复：发布镜像启用 AVI demuxer；现有 MPEG-4 decoder 可处理已抽样文件的视频流
- 升级：migration 15 仅重置 AVI 的媒体探测状态并重新排队 grid 处理任务，不修改原文件
- 不变量：媒体挂载保持只读；失败任务仍有界领取，缩略图和元数据仍为可重建派生状态
- 证据：镜像内 `ffprobe` 读取真实 AVI、迁移后任务状态、完整 Go/前端测试与容器健康检查
