# FIX-2026-07-31：媒体库列表自适应主区域宽度

- 类型：已批准管理中心/UIF 切片内的例行响应式修复
- 影响：管理中心“媒体库”有数据时的标题、列表和分页区域
- 问题：页面使用 `--size-settings-content` 固定最大宽度，宽屏下列表长期停留在窄列
- 修复：移除媒体库列表页的固定最大宽度，保留随视口变化的安全页边距并占满管理主区域
- 不变量：移动端单列、卡片操作换行、空状态和其他管理页面宽度规则不变
- 回归：媒体库页面组件测试、类型检查、前端架构检查与生产构建
