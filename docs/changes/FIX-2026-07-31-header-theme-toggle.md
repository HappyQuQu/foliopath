# FIX-2026-07-31：Header 快速主题切换

- 类型：已批准应用壳/UIF 切片内的例行交互优化
- Gate：沿用已交付的双主题与认证后全局 Header，不新增后端或 API 合同
- 影响：所有认证后页面的全局 Header
- 不变量：`ThemeProvider` 与共享 `ThemeToggle` 仍是主题状态、持久化和切换行为的唯一 owner
- 行为：在账户菜单左侧增加共享主题图标按钮，可直接在浅色与深色主题间切换
- 响应式：窄屏右侧操作区使用内容自适应列，保留搜索框的可伸缩空间
- 可访问性：按钮沿用中英文动态操作标签、键盘焦点和图标隐藏语义
- 回归：应用壳组件测试验证按钮顺序、主题切换及根节点主题状态
